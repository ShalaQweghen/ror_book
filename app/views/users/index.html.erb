<section class="container">
	<ul class="user-list">
		<% @users.each do |user| %>
			<li class="user-item"><%= image_tag("/images/Untitled.png", size: "40") %> <%= link_to user.username, user_path(user.id) %></li>
		<% end %>
	</ul>
</section>